static constexpr int N = 1; // # of dimensions

#include <cmath> //for trig
#include <numbers> //for pi

int main(int argc, char * const argv[])
{
    // --------------------------------------------------------------------
    // Define temporal parameters.
    // --------------------------------------------------------------------
    int step = 0;
    double t = 0;
    double dtRK = 0;

    // inputs.getButcherTableau()

    return 0;
}